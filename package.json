{
  "name": "opengine-monorepo-poc",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build --cache-dir=.turbo",
    "build:apps": "turbo run build --filter=./apps/* --cache-dir=.turbo",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "preview": "turbo run preview",
    "start:app": "turbo run start --filter=./apps/*"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@netmanagement/eslint-config": "workspace:*",
    "@netmanagement/typescript-config": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "^1.11.0"
  },
  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=8.11.0"
  },
  "packageManager": "pnpm@8.11.0"
}
